// const myNumber = -5;
// const output = Math.abs(myNumber);

const myNumber = 2.638598;
const output = Math.ceil(myNumber);
const outputFloor = Math.floor(myNumber);
const outputRound = Math.round(myNumber);

const outputRandom = Math.random() * 10;
const randomRound = Math.round(outputRandom);

console.log('celling Number test---', output);
console.log('floor number test----', outputFloor);
console.log('round number test----', outputRound);
console.log('random Number round test----', randomRound);